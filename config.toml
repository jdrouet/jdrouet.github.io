title = "👋 Welcome"
description = "Here are my projects, my thoughts and other things."

# The URL the site will be built for
# base_url = "https://jdrouet.github.io"
base_url = "/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# When set to "true", the generated HTML files are minified.
minify_html = true

# When set to "true", a feed is automatically generated.
generate_feeds = true

author = "Jérémie Droue<jeremie.drouet@gmail.com>"

theme = "solar-theme-zola"

[markdown]
# extra_syntaxes_and_themes = ["highlight_themes"]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "solarized-dark"

[extra]
# Put all your custom variables here
site_menus = [
    { url = "https://mamot.fr/@jdrouet", name = "Mastodon" },
    { url = "https://github.com/jdrouet", name = "Github" },
]
